<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:statusBarColor">@color/colorAppTheme</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- DialogLargeImage theme -->
    <style name="DialogLargeImage" parent="@android:style/Theme.Dialog">
        <item name="android:windowEnterAnimation">@anim/anim_poster_in</item>
        <item name="android:windowExitAnimation">@anim/anim_poster_out</item>
    </style>

    <!-- Login EditText theme -->
    <style name="LoginEditTextTheme">
        <!-- Used for the bottom line when not selected / focused -->
        <item name="colorControlNormal">@color/colorLightGray</item>
        <item name="colorAccent">@color/colorLightBlue</item>
    </style>

    <style name="CustomTabLayout" parent="Base.Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/MyTabTextAppearance</item>
    </style>

    <style name="MyTabTextAppearance" parent="TextAppearance.AppCompat.Button">
        <item name="android:textSize">12sp</item>
        <item name="textAllCaps">false</item>
        <item name="fontFamily">@font/source_sans_pro_bold</item>
    </style>

</resources>